<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="upr.uas.vivi">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VAlpheus"
        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.VAlpheus.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".crud.brand.CreateBrandActivity" />
        <activity android:name=".crud.brand.UpdateBrandActivity" />
        <activity android:name=".crud.produk.CreateProdukActivity" />
        <activity android:name=".crud.produk.UpdateProdukActivity" />
        <activity android:name=".crud.transaksi.CreateTransaksiActivity" />
        <activity android:name=".crud.transaksi.UpdateTransaksiActivity" />
        <activity android:name=".crud.pembayaran.CreatePembayaranActivity" />
        <activity android:name=".crud.pembayaran.UpdatePembayaranActivity" />
    </application>

</manifest>